organization.name(*),name(*),label,description,objectType.shortName(*),type(*),menu.name,enableRoles,extInfo
;; Line begin with ; will be treat as comments
;; Use $ROOT_ORG$ to refer to the root organization
